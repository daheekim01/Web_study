## 🪆서버사이드 렌더링(SSR)과 클라이언트 사이드 렌더링(CSR)의 구분하기

서버에서 어떻게 구분하냐면, **서버가 어떤 방식으로 HTML을 렌더링해서 클라이언트에 전달하는지**에 따라 다릅니다.

#### 1. **서버사이드 렌더링(SSR)**

* 서버는 클라이언트가 요청한 URL에 맞는 **완전한 HTML 페이지**를 생성해서 보내줍니다. 이 HTML에는 이미 필요한 콘텐츠가 모두 포함되어 있습니다.
* 예를 들어, 서버에서 페이지를 요청할 때 `index.html`을 반환하는데, 그 안에 **실제 텍스트, 이미지, 콘텐츠**가 모두 이미 렌더링 되어 있는 거죠.

#### 2. **클라이언트 사이드 렌더링(CSR)**

* 서버는 클라이언트가 요청한 URL에 대해 **빈 HTML 틀**만 보냅니다. 예를 들어, `<div id="app"></div>`와 같이 빈 div 태그만을 보냅니다.
* 그 다음, 클라이언트는 이 빈 HTML을 받아서 **자바스크립트**를 실행시킵니다. 자바스크립트는 서버에 추가 요청을 보내서 데이터를 가져오고, 그 데이터를 바탕으로 클라이언트에서 **동적으로** HTML을 만들어서 화면에 표시합니다.

### **서버에서의 구분**

서버에서 SSR과 CSR을 구분하는 방법은 보통 **어떤 방식으로 HTML을 생성하는지**에 따라 다릅니다.

#### 예시: Node.js와 Express에서의 구분

1. **SSR 방식 (서버사이드 렌더링)**:
   서버에서는 페이지를 요청받을 때, 그 요청에 맞는 **완전한 HTML**을 만들어서 반환합니다.

   ```javascript
   app.get('/about', (req, res) => {
     const htmlContent = renderAboutPage(); // About 페이지를 서버에서 렌더링
     res.send(htmlContent);  // 완전한 HTML을 클라이언트에 전달
   });
   ```

2. **CSR 방식 (클라이언트 사이드 렌더링)**:
   서버는 빈 HTML 파일을 보내고, 클라이언트에서 자바스크립트 파일을 실행해 콘텐츠를 동적으로 로드합니다.

   ```javascript
   app.get('/about', (req, res) => {
     res.sendFile(path.join(__dirname, 'index.html'));  // 빈 HTML 파일 전달
   });
   ```

   클라이언트에서 이 HTML을 받아 **자바스크립트** 파일을 실행시킵니다. 그 후, 자바스크립트는 API를 통해 데이터를 가져와 페이지를 렌더링합니다.
   
---

## ✅ **클라이언트 자바스크립트가 실행되어 콘텐츠를 동적으로 로드하고 렌더링된다는 건 무엇인가?**

클라이언트 사이드 렌더링(CSR)에서 "자바스크립트가 실행되어 콘텐츠를 동적으로 로드하고 렌더링된다"는 말은, **웹 페이지를 처음 로딩할 때 HTML이 거의 빈 틀만 오고, 그 후에 자바스크립트가 실행되면서 필요한 콘텐츠(예: 텍스트, 이미지, 사용자 인터페이스 등)를 동적으로 채운다**는 뜻이에요.

#### 예시로 풀어볼게요:

1. **클라이언트가 페이지를 요청**하면 서버는 HTML 파일을 보냅니다. 그런데 이 HTML 파일에는 실제 콘텐츠가 포함되어 있지 않아요. 예를 들어, `<div id="content"></div>`와 같은 빈 div 태그만 있을 수 있어요.
2. 클라이언트(브라우저)에서는 이 빈 div를 화면에 표시하고, 그 다음에 **자바스크립트**가 실행됩니다.
3. 자바스크립트는 **API 요청**을 통해 서버에서 데이터를 받아와서, 그 데이터를 받아 HTML의 빈 div 태그 안에 추가해요. 예를 들어, `fetch()`나 `axios()`를 사용해 서버에서 JSON 형태로 데이터를 받아온 뒤, 자바스크립트를 사용해 이를 화면에 동적으로 렌더링하는 방식이죠.

이 과정을 간단히 요약하면:

* **HTML**: 빈 틀만 클라이언트로 전달
* **자바스크립트**: 데이터를 동적으로 받아서 페이지에 내용을 추가

---

## ✅ SSR과 CSR 개념 정리

| 구분  | SSR (Server-Side Rendering)         | CSR (Client-Side Rendering)                |
| --- | ----------------------------------- | ------------------------------------------ |
| 설명  | HTML을 **서버에서 만들어** 클라이언트(브라우저)에 보내줌 | HTML은 틀만 있고, **JS가 실행되면서 클라이언트에서 콘텐츠를 그림** |
| 예시  | 사용자가 URL 요청 → 서버에서 완성된 페이지 보내줌      | 사용자가 URL 요청 → 기본 틀만 오고, JS로 내용 채워짐         |
| 속도  | 첫 로딩이 빠름 (이미 다 만들어져서 옴)             | 첫 로딩이 느릴 수 있음 (JS 실행 후 렌더링됨)               |
| SEO | 검색엔진 최적화에 유리함                       | SEO에 불리할 수 있음 (크롤러가 JS 실행을 못 할 수 있어서)      |

---

## 🔥 그럼 공격(문구 조작 등)은 어디에 더 취약할까?

이런 공격은 보통 **XSS (Cross-Site Scripting)** 의 일종인데요,
사용자가 입력한 데이터를 검증 없이 HTML에 넣어버리면,
해커가 `<script>` 나 악성 코드 같은 걸 심을 수 있어요.

### ✔️ 중요한 건 "어디서 렌더링하느냐"보다

\*\*"입력값을 어떻게 처리하느냐"\*\*가 핵심입니다.

---

## ✅ CSR vs SSR 공격 취약점 비교

| 항목       | CSR (클라이언트 렌더링)                        | SSR (서버 렌더링)                                      |
| -------- | -------------------------------------- | ------------------------------------------------- |
| 보안취약점 예시 | JS 로직에서 사용자 입력을 innerHTML로 삽입 → XSS 위험 | 서버에서 사용자 입력을 HTML로 만들어 보내줄 때 escaping 안 하면 XSS 위험 |
| 취약성 정도   | 둘 다 XSS에 취약할 수 있음                      | 둘 다 XSS에 취약할 수 있음                                 |
| 특징       | JS에서 처리 잘못하면 클라이언트가 해커 스크립트 실행         | 서버에서 HTML 생성 시 필터링 안 하면 브라우저가 해커 스크립트 실행          |

---

## 📌 핵심 요약

* **SSR**: 서버가 HTML을 완성해서 보냄
* **CSR**: 클라이언트가 JS로 HTML을 완성함
* **공격 위험(XSS)**: SSR, CSR 모두 사용자 입력을 제대로 검증/필터링 안 하면 위험
* **문구 조작, 악성 스크립트 삽입 같은 공격은 보통 XSS 때문**
* **렌더링 방식 자체보다 보안 코딩이 더 중요**

  * 예: `userInput`을 그대로 `<div>${userInput}</div>` 에 넣으면 XSS 가능

---

## 💡 예시 (XSS 코드 삽입)

```javascript
const userInput = '<script>alert("해킹됨")</script>';
document.body.innerHTML = userInput; // ❌ 이렇게 하면 XSS 발생
```

→ 이건 **CSR 방식에서의 문제지만**,
서버에서도 이렇게 보내면 (SSR 방식으로) 똑같이 위험해요.

---

## ✅ 결론

* SSR, CSR 모두 **XSS 공격에 취약할 수 있음**
* 취약한 이유는 **렌더링 방식 자체가 아니라**,
  **입력값 검증/필터링 부족 때문**
* SSR은 서버에서 HTML 생성 시 조심해야 하고
  CSR은 JS로 DOM 조작할 때 조심해야 해요.




